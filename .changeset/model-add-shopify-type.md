---
"ggt": minor
---

Add Shopify model enable support to `ggt model add`.

You can now run `ggt model add <path> --type shopify` (with optional
`--resource <shopifyModelKey>`) to enable Shopify models.
