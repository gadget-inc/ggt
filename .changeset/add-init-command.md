---
"ggt": minor
---

Add `ggt init` command for scaffolding new Gadget applications locally

Creates a new Gadget app (from a template or by forking), pulls its files to a
local directory, generates `.gitignore` and `.ignore` defaults, and initializes
a git repository with an initial commit.
