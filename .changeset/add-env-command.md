---
"ggt": minor
---

Add `ggt env` command for managing environments

Introduces the `ggt env` command with subcommands to list, create, rename, activate, and delete development environments for a Gadget application.
